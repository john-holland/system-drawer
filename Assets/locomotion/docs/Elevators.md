# Elevators

`ElevatorVehicleRagdoll` — door open/close topology, optional bath/kitchen, nested bays (houses/vehicles), `ElevatorButtonPanel` PixelLight grid → `CallFloor`, `ElevatorFloorTravelPlanNode` for SG3D/SG4D shaft travel.
