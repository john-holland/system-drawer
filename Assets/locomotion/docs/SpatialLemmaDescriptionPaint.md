# Spatial Lemma Description Paint

**Existing:** thesaurus adj/adv POS; `ShaderGrammarIndex` adjective→shader; `spatial-generator-definitions` specs; stylesheet skinKeys.

**`SpatialDescriptionComponent`:** `PaintFromModifiers` → painted keys + `SpatialDescriptionFilter` + reflected ShaderGrammarIndex material floats. Property keys: `spatial-description`, `spatial-skin-key`, `spatial-adj-paint`.
