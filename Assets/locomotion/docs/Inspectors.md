# Inspectors

`InspectorBioRhythm` — persona schedules with month/year cron across elevators, planes, trains, buses, automobiles, spaceships, helicopters, boats.

Cards: `InspectorKnockCard`, `InspectorTravelOptionCard` (inspector→vehicle vs vehicle→garage; boats always inspector-goes), `JusticeDogCard` (owner coefficient via `Ragdoll.OpinionFor`).
