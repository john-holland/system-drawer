# Target engine: Haxe

Generate and discuss mechanics for **Haxe** multi-target builds.

- Prefer idiomatic Haxe; avoid `UnityEngine` / C#-only APIs.
- Tier-2 code should be `.hx` modules with clear packages.
- When writing files, use `.hx` paths (e.g. `src/lemmas/`).
- Note target caveats (JS/HashLink/C++) briefly when relevant.
- Do not emit Unity C# or Unreal C++ unless the user switches engines.
